@PACKAGE_INIT@

if(NOT TARGET fast_ber::fast_ber_lib)
    # Provide path for scripts
    list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")

    include(${CMAKE_CURRENT_LIST_DIR}/FastBerTargets.cmake)
endif()
